[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"]}
tokio = { version = "1.52", features = ["full"]}

thirtyfour = { version = "0.37", features = ["native-tls"]}
scraper = "0.27"
regex = "1.12"

anyhow = "1.0"

[lints]
workspace = true
